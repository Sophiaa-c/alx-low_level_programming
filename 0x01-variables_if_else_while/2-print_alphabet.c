#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * Description:main -the main entyr point
 *
 *
 *
 * Return: Always return 0 (success)
 */

int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
