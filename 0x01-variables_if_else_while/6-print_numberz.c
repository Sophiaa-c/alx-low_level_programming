#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * Description:main- The entry point
 * init pointer
 * The rest of the code
 * Return: Always return 0 (success)
 */

int main(void)
{
	int y;

	for (y = 48; y < 58; y++)
		putchar(y);
	putchar('\n');
	return (0);
}
