#include <stdio.h>
#include <stdlib.h>
/**
 * main - the main entry point 
 *
 * Return: always 0
 */
int main(void)
{
	char x[100] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", x);
	return (0);
}
